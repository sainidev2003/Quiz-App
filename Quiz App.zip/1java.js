// Add event listeners to the buttons to start the quizzes
const buttons = document.querySelectorAll('button');

buttons.forEach(button => {
    button.addEventListener('click', () => {
        // Redirect to the quiz page or start the quiz here
        // alert('Start Quiz!');
    });
});